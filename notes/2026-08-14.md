# 2026-08-14

- Looked into gRPC retry policies for the payments service.
- `maxAttempts: 5` with exponential backoff seems reasonable.
- Remember to test against the sandbox before rolling out.
- Also: check if the proxy handles server-streaming retries correctly.
